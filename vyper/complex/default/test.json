{ "modes": [ "V >=0.4.0" ], "cases": [ {
    "name": "entry",
    "inputs": [
        {
            "instance": "Main",
            "method": "main",
            "calldata": [
                "Callable.address"
            ]
        }
    ],
    "expected": [
        "10"
    ]
} ],
    "contracts": {
        "Main": "main.vy",
        "Callable": "callable.vy"
    }
}